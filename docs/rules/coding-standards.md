# コーディング規約 Rules

## 1. 方針

- 責務分離を優先する
- 同じ意味の処理経路を増やさない
- ガード節で分岐を浅くする

## 2. 実装

- 1 関数 1 責務にする
- UI と推論ロジックを混ぜない
- 例外は握りつぶさず、扱える形に変換する

## 3. 命名

- 意味が分かる名前を付ける
- 関数名は英語で書く
- `utils.py` や `helper.py` のような曖昧名を避ける
- 値オブジェクトは不正値を閉じ込めたいときだけ使う
